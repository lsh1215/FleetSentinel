# === AI coding agent artifacts ===
# Local tool state, caches, and generated config. Keep the repo free of
# per-developer agent setup so nobody inherits someone else's tooling.

# Claude Code
.claude/

# oh-my-claudecode (OMC)
.omc/

# oh-my-codex (OMX)
.omx/

# Codex CLI
.codex/

# Gajae Code (gjc)
.gjc/

# Timestamped backups left behind when a setup flow rewrites CLAUDE.md
CLAUDE.md.backup.*

# === Personal conversation logs (local only, kept out of the repo) ===
# Daily silver-level conversation records + curated key log + their scaffolding.
# Personal work history — not a portfolio deliverable.
docs/conversation/
scripts/append-daily-log.sh


# === Build / local runtime artifacts ===
# Iceberg local warehouse + docker volume data (regenerated by `make up`)
infra/data/
warehouse/
# Java build
flink-pipeline/target/
*.class
# Python
generator/.venv/
exploration/.venv/
**/.venv/
**/__pycache__/
*.pyc
.pytest_cache/
# Local env overrides
.env.local

# === v3.0 자율주행 데이터셋 (재배포 금지 — 비상업 라이선스) ===
# nuScenes 원본/변환 산출물은 레포에 커밋하지 않는다. docs/data-design-v3.md §12 참고.
# nuScenes 원본 + 변환 산출물(MCAP/rrd)
data/
*.mcap
*.rrd
nuscenes/

# === 프론트엔드 ===
frontend/node_modules/
frontend/dist/
# 실데이터에서 생성한 개발용 픽스처·재생 파일 (nuScenes 비상업 라이선스)
frontend/public/fixture/
frontend/public/rrd/
*.tsbuildinfo

# 개발용 사설 PKI — 개인키가 평문이다. 커밋하지 않는다(SDD L-7).
pki/
exploration/wal-run/

# Maven 빌드 산출물
target/

# proto에서 생성된 Python 스텁은 커밋한다 — grpcio-tools 없이도 재생기를 읽을 수 있어야
# 하고, 생성 후 상대 임포트로 고치는 수동 단계가 있어 재생성이 자동이 아니다.

# 로컬 전용 문서 — 저장소에는 설계 정본(docs/sdd.md · docs/data-design.md)만 둔다.
# 실수로 다시 커밋되지 않게 막는다.
docs/heavy-path-design.md
gateway/README.md
api/README.md
k8s/README.md
