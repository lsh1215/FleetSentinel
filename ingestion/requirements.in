# FleetSentinel v3.0 수집 계층 직접 의존 (핀 고정본은 requirements.txt)
#
# ⚠️ numpy 2 필수 — rerun-sdk 0.23은 numpy 2의 asarray(copy=) 시그니처를 요구한다.
#    numpy 1.26에서는 쿼터니언 배치가 **조용히 누락**되고 경고만 남는다(P1 실측).
#    nuscenes-devkit 1.2.0은 numpy<2.0.0을 선언하지만 실제 판독 경로는 numpy 2에서
#    정상 동작함을 변환·검증 게이트로 확인했다. devkit 업그레이드 시 재확인할 것.
nuscenes-devkit
rerun-sdk
mcap
numpy>=2.1
python-ulid
pytest
